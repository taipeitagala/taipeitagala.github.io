# 專案現況

> 最後更新：**2026-09-06**
> 這份是「現在走到哪」的快照，每次收工更新。
> 設計規則見 `CLAUDE.md`，技術決定與維運步驟見 `README.md`。

## 兩站目前版本

| | commit | 網址 |
|---|---|---|
| 開發站 | `b97f2b8` | `taipei-tagala-web.github.io`（擋搜尋引擎） |
| 正式站 | `ddb2632` | `tagala.org.tw`（正式對外） |

兩站內容一致，**只有 `CNAME` 與 `robots.txt` 刻意分歧**。
推正式站必須 cherry-pick，見 `README.md`「維運手冊 → 推送到正式站」。

## 活動資料現況

`content/events.json` 共 **8 場**（資料本身未動過，只是頁面上的分組會變）：

| 分組 | 場次 |
|---|---|
| 未來 4 場 | 09-08、09-22、10-13、10-27 |
| 過往 4 場 | 08-25、08-11、07-28、07-14 |

> **⚠ 下一個要處理的時間點：2026-09-08**
> 該場過期後，首頁與例會頁需重新分組，並更新場次數字。
> 目前**只能手動改 6 個 HTML 檔**（三語系 × 首頁／例會頁），詳見下方待辦第 1 項。

## 已完成

- 三語系共 15 個內容頁（中／英／日 × 首頁、關於我們、服務計畫、例會/活動、聯絡我們）
- RWD 手機版漢堡選單（滿版選單、鎖捲動、次級選單手風琴機制已備妥但未使用）
- 頁尾 QR code、主導覽五項、語言切換器移除分隔符號
- 聯絡我們頁改版（移除失效的 Google Map 嵌入，改連結按鈕）
- 正式站搬遷完成，`tagala.org.tw` 網域綁定生效、HTTPS 正常
- `sitemap.xml`（15 筆，含 hreflang 互指）、`.gitignore`

## 待辦（依優先序）

1. **重建 `tools/build-pages.py`** ← 阻擋日常維運
   原產生器已遺失（見 `README.md`「產生器腳本已遺失」）。
   重建後，活動過期只要跑一次腳本，不必手動改 6 個檔、也不會漏改某個語系。
   **使用者已知悉此事，決定暫不執行；每次開工請主動提醒。**

2. **過往活動 7 筆的日期與標題** — 待社方提供資料，我方無法自行補

3. **社徽反白版本** — 需從 Brand Center 產生。取得前，頁尾一律用文字社名，不可放社徽

4. **`service.json` 的 `areasOfFocus` 對照** — 標了 `_verify`，待社內確認

5. **`site.json` 中標 `_verify` 的英日文** — 圖片 alt、按鈕文字、欄位標籤、入社洽詢說明、
   各頁 SEO 描述，皆為建站時補寫，需母語人士確認

6. **`404.html` 尚未建立** — `CLAUDE.md` 的檔案結構有列，但檔案不存在

## 已確認的決定（勿再提問）

- 正式網址為 apex `tagala.org.tw`（不用 www）
- 日文 about 頁的「臺北廳大加蚋堡」保留舊字體，屬史料引用
- 中文頁保留 Open Sans（供數字使用），與 CLAUDE.md 字型規則的已確認例外
- 講者肖像可使用於官網長期陳列
- 講者姓名：英文版用中文姓名，不用漢語拼音；外籍講者用英文姓名
- 特公盟、移民工文學獎的官方英文名；日文版用中英對照，不另譯
